1. Clone the repository:

```sh
git clone https://github.com/resendlabs/react-email.git
```

2. Change working directory  to `docs` 
```sh
cd react-email/docs
```

3. Install all dependencies:

```sh
yarn install
```

4. Run local servers and watch for changes:

```sh
yarn dev
```
